import Component from '@glimmer/component';

export default class TicketsTableComponent extends Component {
  // This is currently empty but gives us a place to add
  // component-specific logic if needed later
} 